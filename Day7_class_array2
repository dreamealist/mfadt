int i = 0;

void setup() {
  size(500, 100);
}

void draw() {
  background(0);
  
  while (i < 8) {
    // i < 8 so the loop will run through 8 times.
    fill(255, 150, 150);
    ellipse(i*50, 25, 50, 50) // change the x position for each circle
    i++; // i = i+1;
        
  }
}

